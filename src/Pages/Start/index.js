import { useGlobalStates, actions } from '../../warehouse'

function Start() {
  // const [state, dispatch] = useGlobalStates()
  // if(state.mode) {//tự luận
  // } else {//trắc nghiệm
  // 	if(state.noAnswer != 0)
  // 	else //lỗi
  // }
  // <button>Trở về</button>
}

export default Start
